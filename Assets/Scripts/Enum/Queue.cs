using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UniRx;

public class Queue : MonoBehaviour
{
    // Start is called before the first frame update
    public static ReactiveCollection<string> StateList;
}
